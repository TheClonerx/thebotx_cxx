#ifndef TCX_CH_CHANNEL_FLAGS_HPP
#define TCX_CH_CHANNEL_FLAGS_HPP

namespace ch {

enum Channels {
    none = 0,
    red = 256,
    blue = 2048,
    shield = 64,
    staff = 128,
    mod = 32768
};

} // namespace ch

#endif
